NEXT_PUBLIC_API_URL=http://localhost:3333

# Domínio do cookie de sessão `@lexhub-auth`. Precisa ser IGUAL ao `DOMAIN` da
# API em cada ambiente: o middleware só consegue apagar o cookie gravado pela
# API (sessão expirada ou inválida) se o domain for idêntico.
NEXT_PUBLIC_DOMAIN=localhost
